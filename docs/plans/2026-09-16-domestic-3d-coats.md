# 田园猫 3D 花色预设

状态：范围已确认，部分实现；新增七套 3D 贴图受 Tripo 服务 504 阻塞，未完成整体验收。

## 与总纲的关系

延续 `2026-09-06-cat-color-v2-design.md` 的渲染验证阶段及 `2026-09-16-tripo-cat-study.md`。主图优先 3D，2D 后备的方向已确认。本轮为隔离样板，未替换正式产品渲染，不改变遗传引擎、冻结基线或正式单 HTML 构建。

## 确认范围

- 短毛中华田园猫：橘、奶油、狸花、蓝狸花、黑、蓝、黑白、蓝白、三花、淡三花、玳瑁，共 11 种。
- 白斑随典型预设，暂不做任意白斑面积、玳瑁稀释色、英短或长毛。
- 沿用已认可的体型，典型花纹采用独立贴图，包含眼周、鼻口和耳内的协调配色。不能用固定美术预设反推完整遗传状态。

## 已完成

- `coat-switcher.html`：六类纹路、正常/稀释色、11 项快捷选择、真实模型的角度按钮、加载失败重试和过期加载释放。
- 原橘、狸花、黑白、三花使用现有真实 GLB；另外七种仅显示明确标注的参考图，禁止旋转，不算 3D 已完成。
- 内置 imagegen 编辑同一原橘猫概念图，生成七张花色参考图；完整提示保存在 `reference-prompts.json`。
- GLB 和 PNG 保存在 Git 外归档目录。`asset-manifest.json` 保存大小与 SHA-256，恢复脚本验证后建立链接，不覆盖不同文件。
- 原四猫和对比页先以 `a225110` 提交存档。

## 阻塞及恢复步骤

2026-09-16 浏览器多次显示 `studio.tripo3d.ai` Host Error / 504（包括页面时间 14:16:36 UTC 的错误）；刷新仍失败。另尝试 `tripostudio.com` 入口时导航超时。本轮没有提交 Tripo 生成任务，也没有新增 Tripo 积分消费。

服务恢复后逐套执行：

1. 为七种花色建立各自的原猫模型副本，用对应 `reference-*.png` 生成 4K 贴图，保留现有四套成果。
2. 检查正面、侧面、背面的花纹和面部自然度；纯色不得残留橘斑，玳瑁无白斑，淡三花为蓝/奶油/白。
3. 导出 GLB 4K，归档 `cat-<key>-4k.glb`；用 `tools/cat3d/optimize_tripo.py` 输出约 80k 面 / 2K 的网页版本。
4. 将任务 ID、尺寸、SHA-256 等加入 manifest 的 `assets`。切换页按 key 自动使用真实模型，替换参考图状态；同步完成度文案和计划状态。
5. 验证连续切换、缺失资产重试、角度切换与窄屏；逐套等待用户美术验收后再做正式产品接入。

## 本轮验证

- Node 24 / pnpm 11.5.2：`pnpm run check` 通过，28 个测试文件 / 2268 项测试全部通过，单文件构建通过。
- 新样板独立 TypeScript 检查通过（含 ES2022、DOM、DOM.Iterable）。
- 浏览器逐项切换 11 种花色，真实模型与参考图标识正确；参考图无 canvas、旋转禁用；三花加载后背面视角正常；控制台无 error。
- 390px 窄屏选择区目视检查通过；未做真实手机性能验证。
- 恢复脚本验证四个 GLB 和七张参考图的哈希通过。

## 命名与纯色验收修正

用户确认蓝灰色可以保留。目录称为“11 种花色预设”，不声称所有预设均在国内田园猫中常见。“蓝猫”改为“纯蓝灰色”，避免与英短品种混淆。低多边形三角明暗属于画风，不是毛发斑纹。后续纯色蓝灰贴图需消除尾部环纹感，保留正常光影；不得误做成蓝狸花。

### 后续恢复检查

工作台重新加载后可访问，原模型仍可查看；但当前账户菜单显示免费版，会员详情“我的积分”为 0，使用记录为空。单次生成纹理按钮显示 20 积分。因此新的阻塞是账户积分/登录状态待用户核对，不能继续按旧余额推断可生成。未提交生成、未购买或升级。
